import PageContainer from "../components/PageContainer"

const LogIn = () => {
  const title = "Log In"
  return (
    <PageContainer title={title}>
      <h1>{title}</h1>
    </PageContainer>
  )
}

export default LogIn